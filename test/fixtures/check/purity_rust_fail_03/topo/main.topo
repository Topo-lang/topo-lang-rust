// Purity violation: parallel stage function uses compound assignment on
// a static mut global.

namespace app {
  public:
    void run();
    void tick();
    void monitor();

    fn run {
        stage<1> tick();
        stage<1> monitor();
    }
}
